@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Product")

@foreach (var category in Model)
{
    @Html.RouteLink(
        category,
        new {controller = "Product", action = "List", category, page = 1},
        new {@class = category == ViewBag.SelectedCategory ? "selected" : null})
}