@model IEnumerable<string>

<div>
    This is the message from partial view.
    <ul>
        @foreach (var item in Model)
        {
            <li>@item</li>
        }
    </ul>
    @Html.ActionLink("Go back to the Index", "Index")
</div>
